# Runtime deps for the tape collector + dashboard (pinned to the live box).
# Reference only — this repo is not meant to be run from this checkout.
websocket-client==1.9.0
flask==3.1.3
requests==2.33.1
python-dotenv==1.2.2
